name: ecommerce_warehouse
version: "1.0.0"
config-version: 2

profile: ecommerce_warehouse

model-paths: ["models"]
test-paths: ["tests"]
clean-targets: ["target", "logs"]

models:
  ecommerce_warehouse:
    staging:
      +materialized: view
      +schema: staging
    warehouse:
      +materialized: table
      +schema: warehouse
    marts:
      +materialized: table
      +schema: marts
