resources:
  dashboards:
    ecommerce_analytics_dashboard:
      display_name: Ecommerce Analytics
      warehouse_id: ${var.warehouse_id}
      file_path: ../dashboards/ecommerce_analytics.lvdash.json
      dataset_catalog: ${var.catalog}
      dataset_schema: ${var.gold_schema}
